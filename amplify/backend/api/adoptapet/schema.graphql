type Owner @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  email: AWSEmail
}

enum ItemType {
  SHIRT
  SHORTS
}

type Item @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  description: String
  photo: AWSURL
  ItemType: ItemType
  Owner: Owner @hasOne
}
 